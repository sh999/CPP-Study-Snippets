/*
	Goal: Run multiple cpp files using headers
*/
#include <iostream>
#include "multiply.h"
int main(){
	int x = 3;
	int y = 2;
	multiply(2, 1);
	// std::cout << x << " * " << y << " = " << multiply(2, 1);
}